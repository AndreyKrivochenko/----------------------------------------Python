Для запуска в Linux используем: gunicorn wsgi:app_object
Для запуска в Windows использовал модуль waitress, команда: waitress-serve --listen=*:8000 wsgi:app_object

Жесткое кодирование.
    В модуле data_mapper.py при создании соединения было жестко указано имя файла базы данных
    РЕШЕНИЕ: В проекте создал файл настроек settings.py, в нем завел константу DATABASE_NAME

Слепая вера.
    В модуле pages.py, в контроллере CreateStudentPage не проводились проверки на корректность ввода
    РЕШЕНИЕ: В models.py создал класс для проверок Checks. В TrainingSite написал валидируюший метод.

    В модуле pages.py, в контроллере CoursesPage не проводились проверки на корректность ввода url
    РЕШЕНИЕ: В models.py создал класс для проверок Checks. В TrainingSite написал валидируюший метод.

    Еще есть несколько вводов текстовых данных, не делал, но решается аналогично

Спагетти код.
    В модуле application.py фреймворка, в методе path_processing явно виден перебор со спагетти. Но как от этого
    избавиться пока не понял.